# Data

`Perovskite_database_content_all_data.csv` is the baseline table used by the
default application configuration and downstream PCE/stability workflow.

The literature-expansion corpus, extracted PDFs, evidence packets, model
outputs, and manuscript analysis artifacts are intentionally not committed.
Those are generated or locally governed research data. Verify the baseline
dataset's original citation and redistribution terms before public release.
